import React from 'react'
import { View } from 'react-native'

interface Props {
  color: string;
  ar: number;
  ab: number;
  der: number;
  izq: number;
}

export const Circulos = ({ color, ar, ab, der, izq }: Props) => {
  return (
    <View>
      
    </View>
  )
}
